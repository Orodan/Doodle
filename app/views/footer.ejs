
<form action="/choose-language" method="get">
	<select name="language">
		<option value="en"><%= __('English') %></option>
		<option value="fr"><%= __('French') %></option>
	</select>
	<button type="submit" class="btn btn-default"><%= __('Submit') %></button>
</form>